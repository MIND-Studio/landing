# Keep the build context small and prevent host artifacts from clobbering
# the image's npm-ci'd node_modules during `COPY . .`.
node_modules
.next
.git
.github

# Secrets and local env.
.env
.env.*
!.env.example

# Misc
*.tgz
*.log
npm-debug.log*
.DS_Store
Dockerfile
.dockerignore
README.md
.playwright-mcp
